# 📋 Подробное описание пунктов меню Stable Node Manager

## Основное меню

---

### 1) 📦 Инициализация окружения сервера

**Что делает:**
- Обновляет список пакетов APT (`apt update`)
- Обновляет все установленные пакеты до последних версий (`apt upgrade -y`)
- Устанавливает необходимые зависимости:
  - `curl` - для скачивания файлов через HTTP/HTTPS
  - `wget` - альтернативный инструмент загрузки
  - `tar` - для распаковки архивов
  - `unzip` - для работы с ZIP-архивами
  - `jq` - для парсинга JSON (используется при проверке RPC)
  - `lz4` - для распаковки снапшотов (формат сжатия)
  - `pv` - для отображения прогресса распаковки

**Для чего нужен:**
Это первый обязательный шаг перед установкой ноды. Подготавливает чистый сервер, устанавливая все необходимые инструменты для работы ноды Stable. Без этих пакетов установка ноды будет невозможна.

**Когда использовать:**
- На новом сервере перед первой установкой
- После переустановки операционной системы
- Если у вас отсутствуют какие-либо зависимости

---

### 2) ⚙️ Развернуть ноду Stable

**Что делает:**

1. **Определение архитектуры:**
   - Автоматически определяет архитектуру процессора (amd64 или arm64)
   - Выбирает соответствующий бинарный файл для загрузки

2. **Запрос имени ноды (Moniker):**
   - Просит ввести УНИКАЛЬНОЕ имя вашей ноды (обязательно, только латиница)
   - Проверяет корректность: только латинские буквы, цифры, дефис (-) и подчеркивание (_)
   - Это имя будет отображаться в сети

3. **Загрузка бинарного файла:**
   - Скачивает официальный бинарь `stabled` версии 1.1.1 с AWS S3
   - Распаковывает и устанавливает в `/usr/bin/stabled`
   - Делает файл исполняемым

4. **Инициализация ноды:**
   - Выполняет `stabled init` с вашим именем и chain-id `stabletestnet_2201-1`
   - Создаёт структуру каталогов в `/root/.stabled`

5. **Загрузка Genesis файла:**
   - Скачивает официальный genesis.json с AWS S3
   - Проверяет контрольную сумму SHA256 для безопасности
   - Ожидаемый hash: `66afbb6e57e6faf019b3021de299125cddab61d433f28894db751252f5b8eaf2`

6. **Настройка конфигурации:**
   - Скачивает предварительно настроенные config.toml и app.toml
   - Применяет ваш moniker в конфиг
   - Настраивает CORS (`cors_allowed_origins = ["*"]`)
   - Добавляет список проверенных пиров (peers)
   - Устанавливает лимиты подключений:
     - Входящие: 50 пиров
     - Исходящие: 30 пиров
   - Включает RPC на портах:
     - JSON-RPC: 8545
     - WebSocket: 8546

7. **Создание systemd сервиса:**
   - Создаёт файл `/etc/systemd/system/stabled.service`
   - Настраивает автоперезапуск при падении (RestartSec=3)
   - Устанавливает лимит открытых файлов (65535)
   - Включает сервис в автозагрузку

8. **Опциональное применение снапшота:**
   - Спрашивает: "Подтянуть снапшот сейчас? [y/N]:"
   - Если вы выберете `y`:
     - Создаёт бэкап текущих данных
     - Скачивает официальный снапшот (~несколько GB)
     - Распаковывает данные блокчейна
     - Это ускоряет синхронизацию с 6-24 часов до 20-40 минут

**Для чего нужен:**
Полная автоматическая установка и настройка ноды Stable от А до Я. После выполнения этого пункта нода будет готова к запуску.

**Когда использовать:**
- Первая установка ноды на сервер
- После полного удаления предыдущей установки
- Один раз на каждый сервер

---

### 3) 🚀 Старт службы ноды

**Что делает:**
- Выполняет команду `systemctl start stabled`
- Запускает сервис ноды в фоновом режиме
- Нода начинает подключаться к пирам и синхронизироваться

**Для чего нужен:**
Запускает ноду после установки или после остановки. После запуска нода начинает работу: подключается к сети Stable, скачивает блоки, валидирует транзакции.

**Когда использовать:**
- Сразу после установки (пункт 2)
- После остановки ноды
- После перезагрузки сервера (хотя сервис настроен на автозапуск)

---

### 4) 📜 Просмотр журнала событий

**Что делает:**
- Выполняет `journalctl -u stabled -f -n 200`
- Показывает последние 200 строк логов
- Флаг `-f` означает режим "follow" - логи обновляются в реальном времени
- Показывает все события: подключения к пирам, синхронизацию блоков, ошибки

**Для чего нужен:**
Мониторинг работы ноды в реальном времени. Здесь вы увидите:
- Процесс синхронизации блоков
- Количество подключённых пиров
- Ошибки и предупреждения
- Текущую высоту блока
- Сообщения об обновлениях версии

**Когда использовать:**
- Для проверки, что нода работает нормально
- При диагностике проблем
- Для отслеживания прогресса синхронизации
- Нажмите `Ctrl+C` для выхода из режима просмотра логов

---

### 5) 📊 Информация о работе ноды

**Что делает:**
- Выполняет `systemctl status stabled`
- Показывает краткую информацию о состоянии сервиса:
  - Статус: active (running) / inactive / failed
  - Время запуска и работы (uptime)
  - PID процесса
  - Использование памяти
  - Последние строки логов

**Для чего нужен:**
Быстрая проверка состояния сервиса без подробных логов. Позволяет убедиться, что нода запущена и работает.

**Когда использовать:**
- Для быстрой проверки статуса
- Чтобы узнать, сколько времени нода работает
- Для проверки, не упал ли сервис

---

### 6) 🔄 Перезапуск службы

**Что делает:**
- Выполняет `systemctl restart stabled`
- Останавливает ноду
- Немедленно запускает её заново
- Все подключения к пирам переустанавливаются

**Для чего нужен:**
Перезагрузка ноды для применения изменений или решения проблем. После рестарта нода:
- Перечитывает конфигурационные файлы
- Очищает память
- Заново подключается к пирам

**Когда использовать:**
- После изменения конфигурации (config.toml, app.toml)
- При проблемах с подключением к пирам
- Если нода "зависла" или работает нестабильно
- После обновления списка пиров

---

### 7) 🗑️ Полное удаление ноды

**Что делает:**

1. **Запрашивает подтверждение:**
   - "Удалить бинарь, сервис и все данные ноды? [y/N]:"
   - Требует явного согласия

2. **Останавливает все сервисы:**
   - Останавливает `stabled.service`
   - Отключает автозапуск сервисов
   - Останавливает таймеры автоснапшотов

3. **Удаляет systemd файлы:**
   - `/etc/systemd/system/stabled.service`
   - `/etc/systemd/system/stable-snapshot.service`
   - `/etc/systemd/system/stable-snapshot.timer`
   - Перезагружает systemd daemon

4. **Завершает процессы:**
   - Мягкое завершение процесса stabled
   - Через секунду принудительное завершение (kill -9)

5. **Снимает блокировки:**
   - Удаляет LOCK файлы из базы данных
   - Освобождает файлы для возможного повторного использования

6. **Удаляет все данные:**
   - Каталог ноды: `/root/.stabled` (все блоки, состояния, конфиги)
   - Бэкапы: `/root/stable-backup`
   - Снапшоты: `/root/snapshot`
   - Временные файлы: `/tmp/stable_genesis`, `/tmp/rpc_cfg`
   - Бинарный файл: `/usr/bin/stabled`
   - Логи: `/var/log/stabled`

**Для чего нужен:**
Полное удаление ноды с сервера со всеми данными. Возвращает сервер в состояние "до установки".

**Когда использовать:**
- Перед переустановкой ноды с нуля
- При выводе ноды из эксплуатации
- Для освобождения места на диске
- **ВНИМАНИЕ:** Это необратимая операция! Все данные блокчейна будут удалены.

---

### 8) 🏷️ Показать версию бинаря

**Что делает:**
- Выполняет команду `stabled version`
- Показывает установленную версию бинарного файла
- Обычно отображает: `1.1.1` или другую актуальную версию

**Для чего нужен:**
Проверка текущей версии ноды. Полезно для:
- Определения, нужно ли обновление
- Подтверждения успешного обновления
- Диагностики проблем совместимости

**Когда использовать:**
- Перед обновлением (чтобы знать текущую версию)
- После обновления (для проверки)
- При сообщениях об устаревшей версии в логах

---

### 9) 💚 Диагностика работоспособности

**Что делает:**

Комплексная проверка здоровья ноды по 5 параметрам:

1. **Статус сервиса:**
   - Проверяет: `systemctl is-active stabled`
   - ✅ Зелёный: Сервис работает
   - ❌ Красный: Сервис остановлен (показывает логи для диагностики)

2. **Статус синхронизации:**
   - Запрос к RPC: `curl localhost:26657/status`
   - Проверяет поле `catching_up`:
     - ✅ Зелёный: `false` - нода синхронизирована
     - ⚠️ Жёлтый: `true` - идёт синхронизация

3. **Количество пиров:**
   - Запрос к RPC: `curl localhost:26657/net_info`
   - Проверяет `n_peers`:
     - ✅ Зелёный: 3+ подключённых пира (хорошо)
     - ⚠️ Жёлтый: < 3 пиров (плохая связность)

4. **Использование диска:**
   - Команда: `df -h /`
   - Проверяет процент занятого места:
     - ✅ Зелёный: < 80% (достаточно места)
     - ⚠️ Жёлтый: ≥ 80% (нужно освободить место)

5. **Использование памяти:**
   - Команда: `free -m`
   - Вычисляет процент использования RAM:
     - ✅ Зелёный: < 80% (достаточно памяти)
     - ⚠️ Жёлтый: ≥ 80% (высокая нагрузка на память)

**Для чего нужен:**
Быстрая диагностика состояния ноды "одним взглядом". Показывает все критические параметры в удобном виде.

**Когда использовать:**
- Через 10-15 минут после запуска ноды (для проверки успешной установки)
- Ежедневно для мониторинга
- При подозрении на проблемы
- Перед применением снапшота (чтобы понять, нужен ли он)

---

### 10) 📸 Быстрая синхронизация (snapshot)

**Что делает:**

1. **Запрашивает подтверждение:**
   - "Будет сброшена БД и применён оф. снапшот. Продолжить? [y/N]:"

2. **Останавливает ноду:**
   - `systemctl stop stabled`

3. **Сброс данных (с сохранением адресной книги):**
   - Выполняет: `stabled comet unsafe-reset-all --keep-addr-book`
   - Удаляет все блоки, состояния, транзакции
   - **Сохраняет** список известных пиров (addrbook.json)

4. **Загрузка снапшота:**
   - URL: `https://stable-snapshot.s3.eu-central-1.amazonaws.com/snapshot.tar.lz4`
   - Размер: несколько гигабайт (зависит от возраста снапшота)
   - Показывает прогресс загрузки

5. **Удаление старых данных:**
   - Полностью очищает `/root/.stabled/data`

6. **Распаковка снапшота:**
   - Использует `pv` для отображения прогресса
   - Распаковывает с помощью lz4
   - Извлекает в `/root/.stabled/`

7. **Перезапуск ноды:**
   - `systemctl start stabled`
   - Ждёт 10 секунд
   - Проверяет статус синхронизации и высоту блока

**Для чего нужен:**
Быстрая синхронизация ноды до актуального состояния сети. Вместо загрузки всех блоков с нуля (6-24 часа), вы получаете готовое состояние (20-40 минут).

**Когда использовать:**
- При первой установке (если не применили снапшот в пункте 2)
- Если нода сильно отстала от сети (catching_up долго `true`)
- После долгого простоя ноды
- При проблемах с синхронизацией
- Для экономии времени и трафика

**Примерное время:** 15-30 минут в зависимости от скорости интернета

---

### 11) ⏰ Планировщик автосинхронизации

**Что делает:**

Подменю для управления автоматическим применением снапшотов:

#### 1) ⏰ Install/Update daily timer
**Что делает:**
- Скачивает helper-скрипт `stable-apply-snapshot.sh` (если отсутствует)
- Запрашивает время для ежедневного запуска (по умолчанию: 00:15)
- Создаёт systemd service: `/etc/systemd/system/stable-snapshot.service`
- Создаёт systemd timer: `/etc/systemd/system/stable-snapshot.timer`
- Настраивает `OnCalendar` на указанное время
- Включает и запускает таймер
- Добавляет случайную задержку до 5 минут (`RandomizedDelaySec=300`)

**Для чего:** Автоматизация ежедневного обновления ноды через снапшот. Гарантирует, что ваша нода всегда актуальна.

#### 2) ⛔ Disable timer
**Что делает:**
- Останавливает таймер
- Отключает автозапуск таймера
- Не удаляет файлы, просто деактивирует

**Для чего:** Временно или навсегда отключить автоматические снапшоты.

#### 3) ▶️ Run snapshot now (oneshot)
**Что делает:**
- Немедленно запускает сервис `stable-snapshot.service`
- Выполняет те же действия, что и пункт 10 главного меню
- Не ждёт расписания таймера

**Для чего:** Ручной запуск снапшота через автоматизированный сервис.

#### 4) 📊 Status
**Что делает:**
- Показывает статус таймера: `systemctl status stable-snapshot.timer`
- Показывает статус сервиса: `systemctl status stable-snapshot.service`
- Отображает расписание следующих запусков: `systemctl list-timers`
- Показывает логи последних выполнений

**Для чего:** Проверка работы автоматизации, просмотр истории запусков.

#### 5) 🔄 Подтянуть/обновить helper из GitHub
**Что делает:**
- Скачивает последнюю версию `stable-apply-snapshot.sh` с GitHub
- Сохраняет в `/usr/local/bin/stable-apply-snapshot.sh`
- Делает файл исполняемым

**Для чего:** Обновление скрипта автоснапшотов при наличии исправлений или улучшений.

**Когда использовать меню 11:**
- Настройте таймер один раз после установки ноды
- Проверяйте статус периодически
- Обновляйте helper при появлении новых версий

---

### 12) 🌐 Актуализация списка пиров

**Что делает:**

1. **Показывает текущие пиры:**
   - Читает `persistent_peers` из config.toml
   - Отображает список текущих подключений

2. **Запрашивает новые пиры:**
   - Вы можете вставить свой список пиров (формат: `nodeID@ip:port,nodeID@ip:port`)
   - Или нажать Enter - тогда используются резервные пиры (10 нод)

3. **Создаёт бэкап конфига:**
   - Копирует config.toml с меткой времени
   - Например: `config.toml.bak.2025-11-19_14-30-45`

4. **Записывает новую конфигурацию:**
   - Удаляет старые настройки peers из секции [p2p]
   - Добавляет новые настройки:
     - `seeds = ""`
     - `persistent_peers = "список_ваших_пиров"`
     - `persistent_peers_max_dial_period = "0s"`
     - `pex = true` (включает обмен адресами пиров)
     - `addr_book_strict = false` (менее строгая проверка адресов)

5. **Перезапускает ноду:**
   - `systemctl restart stabled`
   - Ждёт 3 секунды

6. **Проверяет результат:**
   - Запрашивает количество подключённых пиров через RPC
   - Проверяет статус синхронизации
   - Выводит результаты

**Для чего нужен:**
Улучшение связности ноды с сетью. Если у вас мало пиров или проблемы с подключением, обновление списка пиров решает проблему.

**Когда использовать:**
- При низком количестве пиров (< 3)
- Если нода не синхронизируется
- При проблемах с подключением к сети
- Когда старые пиры не отвечают
- Периодически для обновления списка активных нод

**Резервные пиры (используются по умолчанию):**
```
e8dc4eb1aed53078d90209c7d8d19d10e79e40bb@62.84.184.22:26656
babe0a3c95868b13cafe31d3473ab646268b7ceb@217.76.62.42:26656
9b9897064ed6a27f3e44d3269ebe9bc06e1ba233@217.76.55.225:26656
91947248cd012523a7b8cfe40791e09465031396@38.242.158.172:26656
86dab3dc399c33ff9770fd089f51125d004a2fe3@130.185.118.7:26656
0bbbed1c8c054f66d45e81a11520456f42e7fca7@45.8.132.10:26656
68a099f9fcf3a3fcff6e549105d206125b51d973@62.169.31.251:26656
0a09e1e1c96f3e8be3204bf10d35ac64294ca826@109.199.108.76:26656
5db9f874c394590ce45c7d946fbd1a1afbf01d21@5.189.136.244:26656
d941c5214bd00b2652a638397c1121fa4a51eae4@89.117.58.6:26656
```

---

### 13) 🤖 Интеллектуальное обновление

**Что делает:**

1. **Сканирует логи:**
   - Выполняет: `journalctl -u stabled -n 2000`
   - Ищет в логах сообщения вида: `Upgrade to v1.2.0`
   - Извлекает номер версии

2. **Определяет версию для обновления:**
   - Если версия найдена в логах - использует её
   - Если не найдена - спрашивает вручную (по умолчанию: 1.1.1)

3. **Выполняет обновление:**
   - Останавливает ноду: `systemctl stop stabled`
   - Создаёт бэкап текущего бинаря с меткой времени
   - Определяет архитектуру (amd64/arm64)
   - Скачивает новый бинарь с AWS S3
   - URL: `https://stable-testnet-data.s3.us-east-1.amazonaws.com/stabled-{VERSION}-linux-{ARCH}-testnet.tar.gz`
   - Распаковывает архив
   - Заменяет старый бинарь новым
   - Делает файл исполняемым
   - Запускает ноду: `systemctl start stabled`
   - Проверяет новую версию и статус

**Для чего нужен:**
Автоматическое обновление до версии, которую требует сеть. Когда сеть переходит на новую версию, в логах появляется сообщение об обновлении. Этот пункт упрощает процесс.

**Когда использовать:**
- Когда в логах видите сообщения типа "Upgrade to v1.x.x"
- При рекомендации обновления от команды Stable
- Когда нода не может синхронизироваться из-за устаревшей версии

**Преимущества:**
- Автоматическое определение нужной версии
- Создаётся бэкап для возможности отката
- Минимум ручных действий

---

### 14) ⬆️ Установить целевую версию

**Что делает:**

То же самое, что пункт 13, но **без автоопределения версии**.

1. **Запрашивает версию:**
   - Вы вводите конкретную версию (например: `1.2.0`)

2. **Выполняет обновление:**
   - Все шаги идентичны пункту 13
   - Останавливает ноду
   - Создаёт бэкап
   - Скачивает указанную версию
   - Устанавливает
   - Запускает

**Для чего нужен:**
Обновление до конкретной версии, когда вы знаете, какая именно версия нужна.

**Когда использовать:**
- Для установки определённой версии
- Когда пункт 13 не может определить версию из логов
- При ручном обновлении по инструкции команды проекта
- Для отката на более старую версию (downgrade)

**Пример использования:**
- В Discord/Twitter команда объявила: "Обновите ноды до версии 1.2.5"
- Вы выбираете пункт 14
- Вводите: `1.2.5`
- Нода обновляется

---

### 15) ⬇️ Восстановить предыдущую версию

**Что делает:**

1. **Ищет бэкапы:**
   - Ищет файлы вида: `/usr/bin/stabled.bak-YYYYMMDD-HHMMSS`
   - Сортирует по времени создания
   - Выбирает самый последний бэкап

2. **Проверяет наличие бэкапа:**
   - Если бэкапов нет - выводит ошибку "Бэкапов не найдено"
   - Если есть - продолжает

3. **Выполняет откат:**
   - Останавливает ноду: `systemctl stop stabled`
   - Копирует бэкап обратно в `/usr/bin/stabled`
   - Делает файл исполняемым
   - Запускает ноду: `systemctl start stabled`
   - Показывает восстановленную версию

**Для чего нужен:**
Возврат к предыдущей рабочей версии, если новая версия вызвала проблемы.

**Когда использовать:**
- После обновления нода не запускается
- Новая версия работает нестабильно
- Появились критические ошибки после обновления
- Несовместимость с текущим состоянием сети

**Важно:**
- Откат возможен только если вы обновлялись через пункты 13 или 14
- Они автоматически создают бэкапы
- Можно откатиться только на последнюю предыдущую версию
- Для отката на более старые версии используйте пункт 14

---

### 16) 💾 Архивация критических данных

**Что делает:**

1. **Проверка установки:**
   - Проверяет наличие директории `/root/.stabled/`
   - Если ноды нет - выдаёт предупреждение

2. **Создание временной папки:**
   - Создаёт директорию `/root/stable_backups/backup_YYYY-MM-DD_HH-MM-SS/`
   - Формат имени с датой и временем для уникальности

3. **Копирование критически важных файлов:**
   - 🔑 `priv_validator_key.json` - **САМЫЙ ВАЖНЫЙ!** Приватный ключ валидатора
   - 🔑 `node_key.json` - Ключ ноды для P2P подключений
   - 📊 `priv_validator_state.json` - Состояние валидатора (защита от double-sign)
   - ⚙️ `config.toml` - Все настройки ноды
   - ⚙️ `app.toml` - Настройки приложения
   - 📇 `addrbook.json` - Адресная книга пиров

4. **Архивация:**
   - Создаёт сжатый архив `stable_backup_YYYY-MM-DD_HH-MM-SS.tar.gz`
   - Удаляет временную папку
   - Оставляет только архив

5. **Информация о сохранении:**
   - Показывает путь к созданному архиву
   - Даёт команду для скачивания на локальный ПК

**Для чего нужен:**

Защита от потери доступа к ноде в случае:
- 💥 Сбоя сервера (отказ диска, сбой ОС)
- 🔥 Случайного удаления файлов
- 🌪️ Повреждения данных
- 🚀 Миграции на новый сервер
- 🔄 Переустановки системы

**Когда использовать:**

1. **Обязательно:**
   - ✅ Сразу после первой установки ноды (пункт 2)
   - ✅ После любых критических изменений конфигурации
   - ✅ Перед обновлением системы
   - ✅ Перед применением снапшота (пункт 10)

2. **Рекомендуется:**
   - 📅 Регулярно (раз в неделю или месяц)
   - 🔧 Перед обновлением ноды (пункты 13, 14)
   - 🛠️ Перед любыми экспериментами с настройками

3. **На всякий случай:**
   - Просто периодически для спокойствия

**Техническая информация:**

```bash
# Структура бекапа:
/root/stable_backups/
└── stable_backup_2024-11-20_15-30-45.tar.gz
    ├── priv_validator_key.json    (~300 bytes)
    ├── node_key.json               (~200 bytes)
    ├── priv_validator_state.json  (~150 bytes)
    ├── config.toml                 (~25 KB)
    ├── app.toml                    (~20 KB)
    └── addrbook.json               (~5-50 KB)

# Общий размер архива: ~50-100 KB
```

**Как скачать бекап на свой компьютер:**

После создания бекапа скрипт покажет команду вида:
```bash
scp root@ВАШ_IP_СЕРВЕРА:/root/stable_backups/stable_backup_2024-11-20_15-30-45.tar.gz ./
```

**На Windows** используйте WinSCP или команду в PowerShell:
```powershell
scp root@YOUR_SERVER_IP:/root/stable_backups/stable_backup_*.tar.gz C:\backups\
```

**Восстановление из бекапа:**

Если нужно восстановить ноду:
```bash
# 1. Распаковать архив
cd /root/stable_backups/
tar -xzf stable_backup_2024-11-20_15-30-45.tar.gz

# 2. Остановить ноду
systemctl stop stabled

# 3. Восстановить файлы
cp backup_2024-11-20_15-30-45/priv_validator_key.json /root/.stabled/config/
cp backup_2024-11-20_15-30-45/node_key.json /root/.stabled/config/
cp backup_2024-11-20_15-30-45/priv_validator_state.json /root/.stabled/data/
cp backup_2024-11-20_15-30-45/config.toml /root/.stabled/config/
cp backup_2024-11-20_15-30-45/app.toml /root/.stabled/config/

# 4. Запустить ноду
systemctl start stabled
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**

1. **Храните бекапы в БЕЗОПАСНОМ месте:**
   - ✅ На локальном компьютере (зашифрованный диск)
   - ✅ В облачном хранилище (зашифрованный архив)
   - ✅ На внешнем накопителе (USB)
   - ❌ НЕ храните только на сервере!

2. **Потеря `priv_validator_key.json` означает:**
   - ❌ Полную потерю доступа к ноде
   - ❌ Невозможность восстановления
   - ❌ Потерю всех наград (если будут)

3. **НЕ делитесь бекапами:**
   - ❌ Не отправляйте никому
   - ❌ Не публикуйте в интернете
   - ❌ Не храните в публичных местах

**Примеры использования:**

```bash
# Сценарий 1: После установки
2) ⚙️  Развернуть ноду Stable
3) 🚀 Старт службы ноды
16) 💾 Архивация критических данных ← СРАЗУ ПОСЛЕ УСТАНОВКИ!

# Сценарий 2: Перед снапшотом
16) 💾 Архивация критических данных
10) 📸 Быстрая синхронизация

# Сценарий 3: Регулярное обслуживание
16) 💾 Архивация критических данных (раз в неделю)
```

**Когда НЕ использовать:**
- ❌ Если нода не установлена (пункт 2 еще не выполнен)
- ❌ Нет особого смысла делать 10 бекапов в день

---

### 0) ❌ Завершить работу

**Что делает:**
- Завершает работу скрипта
- Возвращает вас в командную строку

**Для чего нужен:**
Выход из меню управления нодой.

**Когда использовать:**
- После завершения всех операций
- Когда нужно выполнить другие команды в терминале

---

## 🎯 Типичные сценарии использования

### Сценарий 1: Первая установка ноды
1. `1) 📦 Инициализация окружения сервера` - установка зависимостей
2. `2) ⚙️ Развернуть ноду Stable` - установка и настройка (выбрать `y` для снапшота)
3. `3) 🚀 Старт службы ноды` - запуск
4. Подождать 10-15 минут
5. `9) 💚 Диагностика работоспособности` - убедиться, что всё работает

### Сценарий 2: Нода отстала от сети
1. `9) 💚 Диагностика работоспособности` - проверить catching_up
2. Если catching_up долго `true`:
3. `10) 📸 Быстрая синхронизация` - быстрая синхронизация
4. `6) 🔄 Перезапуск службы`
5. `9) 💚 Диагностика работоспособности` - проверить результат

### Сценарий 3: Мало пиров
1. `9) 💚 Диагностика работоспособности` - увидели < 3 пиров
2. `12) 🌐 Актуализация списка пиров` - обновить список (нажать Enter для резервных)
3. Подождать 1-2 минуты
4. `9) 💚 Диагностика работоспособности` - проверить количество пиров

### Сценарий 4: Требуется обновление
1. `4) 📜 Просмотр журнала событий` - увидели сообщение "Upgrade to v1.x.x"
2. `13) 🤖 Интеллектуальное обновление` - обновиться автоматически
3. Или `14) ⬆️ Установить целевую версию` - указать версию
4. `8) 🏷️ Показать версию бинаря` - проверить новую версию
5. `9) 💚 Диагностика работоспособности` - убедиться в работоспособности

### Сценарий 5: Настройка автоматизации
1. `11) ⏰ Планировщик автосинхронизации`
2. Выбрать `1) Install/Update timer`
3. Указать время (например: 02:00)
4. Выбрать `4) Status` - проверить настройку
5. `0) Back` - вернуться в главное меню

---

## 📊 Дополнительная информация

### Важные пути и файлы:
- **Бинарный файл:** `/usr/bin/stabled`
- **Данные ноды:** `/root/.stabled/`
- **Конфиг:** `/root/.stabled/config/config.toml`
- **App конфиг:** `/root/.stabled/config/app.toml`
- **Genesis:** `/root/.stabled/config/genesis.json`
- **Systemd сервис:** `/etc/systemd/system/stabled.service`
- **Helper снапшотов:** `/usr/local/bin/stable-apply-snapshot.sh`

### Полезные команды вне меню:
```bash
# Проверка синхронизации через RPC
curl -s localhost:26657/status | jq .result.sync_info.catching_up

# Текущая высота блока
curl -s localhost:26657/status | jq .result.sync_info.latest_block_height

# Количество пиров
curl -s localhost:26657/net_info | jq .result.n_peers

# Прямой просмотр логов
journalctl -u stabled -f

# Перезапуск ноды
systemctl restart stabled
```

### Мониторинг ресурсов:
Для просмотра детальной статистики используйте дополнительный скрипт:
```bash
bash stable_usage.sh
```

Он покажет:
- Загрузку CPU по ядрам
- Использование RAM процессом и системой
- Детализацию дискового пространства (по папкам)
- Статус синхронизации
- Количество пиров

---

